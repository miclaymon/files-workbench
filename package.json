{
  "name": "files-workbench2",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,green \"cd client && npm run dev:web\" \"cd server/v1 && .venv/bin/uvicorn main:app --reload --host 0.0.0.0 --port 8000\"",
    "dev:v2": "concurrently -n client,server -c cyan,green \"cd client && npm run dev:web\" \"cd server/v2 && PORT=8000 go run .\"",
    "dev:client": "cd client && npm run dev:web",
    "dev:client:electron": "cd client && npm run dev",
    "dev:server": "cd server/v1 && .venv/bin/uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "dev:server:v2": "cd server/v2 && PORT=8000 go run ."
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
