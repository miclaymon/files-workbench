# Go data server base URL used for all read API calls (Explorer, fs.stat, fs.list_dir, etc.)
# Copy this file to .env and adjust as needed. Restart the dev server after changes.
#
# Using 127.0.0.1 instead of localhost keeps API calls on a separate browser
# HTTP connection pool from media requests (thumbnails, previews), which prevents
# thumbnail loading from blocking directory listing requests.
#
# Empty = route through the Nuxt devProxy (browser-only dev; not needed for Electron).
VITE_API_BASE=http://127.0.0.1:8001
